name: native_file
description: Native file API access for Dart — file descriptors, full stat info, fcntl, fallocate, and more. Linux only.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.1

dependencies:
  ffi: ^2.1.0

dev_dependencies:
  args: ^2.6.0
  ffigen: ^20.1.0
  lints: ^6.0.0
  test: ^1.25.6
